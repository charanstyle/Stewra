{
  // What actually ships. Tests are typechecked via tsconfig.json (`npm run typecheck`) but never emitted.
  "extends": "./tsconfig.json",
  "compilerOptions": { "types": ["node"] },
  "exclude": ["dist", "node_modules", "src/tests"]
}
