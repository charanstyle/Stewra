{
  "name": "@stewra/provisioner",
  "version": "0.1.0",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "description": "Stewra Provisioner — the ONLY process with the Docker socket. A tiny HTTP API the backend calls to create/start/stop/destroy hosted-runner containers from a hardcoded, hardened template. The backend can choose WHICH device gets a container; it can never choose what a container is allowed to do.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@stewra/shared-types": "*",
    "undici": "^7.16.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.3",
    "vitest": "^3.2.0"
  }
}
