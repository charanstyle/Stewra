{
  // What actually ships. The tests are typechecked (see tsconfig.json, which `npm run typecheck` uses)
  // but never emitted — a packaged desktop app has no business carrying its own test suite around.
  "extends": "./tsconfig.json",
  "exclude": ["dist", "node_modules", "src/tests"]
}
